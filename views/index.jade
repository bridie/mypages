extends layout

block content
  main#dashboard
    h1(class='hidden') MyPages Dashboard
    section#add-a-page(ng-controller='addPageController')
      h1 Add a page
      form
        input(
          type='text'
          name='pageName'
          placeholder='Name'
          ng-model='formData.pageName'
        )
        input(
          type='url'
          name='pageUrl'
          placeholder='URL'
          ng-model='formData.pageUrl'
        )
        button(
          type='submit'
          ng-click='addPage()'
        ) Add